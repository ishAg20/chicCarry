<%- include('./partials/header') %>

<div
  class="w-full min-h-screen flex justify-center px-4 py-6 sm:px-6 md:px-10 lg:px-20 bg-gray-50"
>
  <div
    class="w-full sm:w-[90%] lg:w-[75%] mx-auto flex flex-col gap-10 bg-white p-6 md:p-8 rounded-lg shadow-md"
  >
    <!-- Heading -->
    <div class="text-center">
      <h1 class="text-2xl font-bold">Manage Saved Addresses</h1>
      <p class="text-gray-600 mt-2">Update or add your delivery addresses.</p>
    </div>

    <!-- Address List -->
    <div class="bg-gray-100 shadow-md p-5 rounded-lg">
      <% if (user.addresses.length > 0) { %>
      <ul class="flex flex-col gap-4">
        <% user.addresses.forEach((address, index) => { %>
        <li class="bg-white p-4 rounded-md border border-gray-300 shadow-sm">
          <p class="font-semibold text-lg"><%= address.label %></p>
          <p class="text-gray-700">
            <%= address.street %>, <%= address.city %> - <%= address.zip %>
          </p>
          <div class="flex gap-3 mt-3">
            <form
              action="/users/account/address/delete/<%= index %>"
              method="POST"
            >
              <button type="submit" class="text-red-500 hover:text-red-700">
                Delete
              </button>
            </form>
          </div>
        </li>
        <% }); %>
      </ul>
      <% } else { %>
      <p class="text-gray-600">
        No saved addresses found. Add a new one below!
      </p>
      <% } %>
    </div>

    <!-- Add Address Form -->
    <div class="bg-white shadow-md p-5 rounded-lg mt-5">
      <h2 class="text-xl font-bold mb-3">Add New Address</h2>
      <form
        action="/users/account/address/add"
        method="post"
        class="grid grid-cols-1 md:grid-cols-2 gap-5"
      >
        <!-- Label -->
        <div>
          <label for="label" class="block text-gray-700 font-semibold"
            >Label (Home / Office ):</label
          >
          <input
            type="text"
            id="label"
            required
            name="label"
            class="w-full border border-gray-300 rounded-md p-2 mt-1"
          />
        </div>

        <!-- Street -->
        <div>
          <label for="street" class="block text-gray-700 font-semibold"
            >Street:</label
          >
          <input
            type="text"
            required
            id="street"
            name="street"
            class="w-full border border-gray-300 rounded-md p-2 mt-1"
          />
        </div>

        <!-- City -->
        <div>
          <label for="city" class="block text-gray-700 font-semibold"
            >City:</label
          >
          <input
            required
            type="text"
            id="city"
            name="city"
            class="w-full border border-gray-300 rounded-md p-2 mt-1"
          />
        </div>

        <!-- State -->
        <div>
          <label for="state" class="block text-gray-700 font-semibold"
            >State:</label
          >
          <select
            required
            id="state"
            name="state"
            class="w-full border border-gray-300 rounded-md p-2 mt-1"
          >
            <option value="" disabled selected>Select State</option>
            <option value="Delhi">Delhi</option>
            <option value="Maharashtra">Maharashtra</option>
            <option value="Karnataka">Karnataka</option>
            <option value="Tamil Nadu">Tamil Nadu</option>
            <option value="Uttar Pradesh">Uttar Pradesh</option>
            <option value="West Bengal">West Bengal</option>
            <option value="Telangana">Telangana</option>
            <option value="Rajasthan">Rajasthan</option>
            <option value="Punjab">Punjab</option>
          </select>
        </div>

        <!-- ZIP Code -->
        <div class="md:col-span-2">
          <label for="zip" class="block text-gray-700 font-semibold"
            >ZIP Code:</label
          >
          <input
            type="text"
            required
            id="zip"
            name="zip"
            class="w-full border border-gray-300 rounded-md p-2 mt-1"
          />
        </div>

        <!-- Submit Button -->
        <div class="md:col-span-2">
          <button
            type="submit"
            class="w-full bg-blue-500 text-white font-bold py-2 px-4 rounded-md hover:bg-blue-600"
          >
            Save Address
          </button>
        </div>
      </form>
    </div>
  </div>
</div>

<%- include('./partials/footer') %>
